# Instalacao local

```bat
python -m venv .venv
.venv\Scripts\activate
python -m pip install --upgrade pip
pip install -r requirements.txt
python run.py
```

Credenciais ficam em `.env`, nunca no repositorio. Use `.env.example` como modelo.
